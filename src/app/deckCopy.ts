/** Shown in the drop zone when no deck is open or the deck path is unknown. */
export const EMPTY_HINT_NO_DECK =
  "Either drop a .md file here, or use Open, New or Tutorial in the header.";

/** Shown when a file is linked but `parseFlashcards` yielded zero `##` cards (new deck or preamble-only). */
export const EMPTY_HINT_LINKED_NO_CARDS = "This deck has no cards yet. Use Add to create one.";
