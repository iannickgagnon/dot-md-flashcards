# Sample deck

Optional preamble before the first card is ignored for navigation but can set context.

## What is a closure?

A **closure** is a function that keeps access to variables from its outer scope after that outer function returns.

## Capital of France?

Paris.

## Name one use for `async`/`await`

Cleaner sequencing of asynchronous operations compared to raw promise chains.

## What does `def foo()` do (Python)?

Use **inline code** for short snippets, e.g. `def foo(): return 1`.

For multi-line examples, use a **fenced** block:

```
def foo():
    return "bar"
```

**Lists** and [example links](https://example.com) work in the answer body too.
